let joia = `colar`;

switch (joia) {
    case `brinco`:
        console.log(`Joia de hoje é ${joia}`);
        break;
        
    case `colar`:
            console.log(`Joia de hoje é ${joia}`);
            break;

    case `anel`:
        console.log(`Joia de hoje é ${joia}`);
        break;

    case `pulseira`:
        console.log(`Joia de hoje é ${joia}`);
        break;


    default:
        console.log(`Sem joia hoje!`);
        break;
}